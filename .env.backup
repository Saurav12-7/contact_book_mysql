# Database Configuration
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DB=contact_book_db

# Application Security
SECRET_KEY=your_super_secret_key_here_change_this_in_production

# Email Configuration (Optional - for Gmail functionality)
# Note: These are set per user in the application, not here
# GMAIL_PASS is deprecated - users configure their own Gmail in Profile Settings

# Production Settings
FLASK_ENV=development
NODE_ENV=development

# Docker specific (when using docker-compose)
# MYSQL_HOST=db
